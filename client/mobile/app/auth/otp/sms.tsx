import { StyleSheet, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

interface SmsProps {
}

export default function SmsScreen({}: SmsProps) {
  return <SafeAreaView>

  </SafeAreaView>;
}

const styles = StyleSheet.create({
  container: {}
});